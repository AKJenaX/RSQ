package com.example.rsq.ai.model

/**
 * Defines the types of hazards that can be detected by the AI system.
 */
enum class HazardType {
    FLOOD,
    FIRE_SMOKE,
    COLLAPSED_STRUCTURE,
    UNKNOWN
}
